use leptos::*;

#[component]
pub fn App(cx: Scope) -> impl IntoView {
    view! { cx,
        <div>
            <h1>ping0 (full Rust)</h1>
            <p>Upload files and get a QR code. Frontend is leptos-based (placeholder).</p>
        </div>
    }
}
